# Multi-Agent AIOps Platform — 项目汇报材料

> 面向 OnCall / SRE 场景的多智能体智能运维诊断平台。
> 基于 FastAPI、LangGraph、RAG、Milvus、MCP 和 DeepSeek / DashScope 兼容大模型构建。

---

## 一、运行演示

### 1.1 一键启动
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\run.ps1
```

`run.ps1` 会依次拉起：
- **基础设施**：Milvus（向量库）+ Redis（会话记忆）+ etcd + MinIO，通过 `docker-compose.yml`
- **5 个 MCP 工具服务**（独立进程，各跑一个端口）：
  - `system_server` (8005) — 本机 CPU/内存/磁盘/进程（psutil）
  - `network_server` (8009) — ping / HTTP / DNS / 端口探测
  - `docker_server` (8011) — Docker ps/stats/logs/inspect
  - `winlog_server` (8008) — Windows 事件日志查询
  - `websearch_server` (8006) — 联网搜索（mock / Tavily / DuckDuckGo）
- **FastAPI 主服务** (9900) — 带热重载

### 1.2 访问点
| 用途 | 地址 |
|---|---|
| Web 前端 | http://localhost:9900 |
| Swagger | http://localhost:9900/docs |
| Milvus UI (Attu) | http://localhost:8000 |

### 1.3 演示
1. 打开前端页面，左侧是 **AIOps 诊断面板**，右侧是 **RAG 聊天面板**
2. 输入一条故障描述，例如：
   - *"我电脑很卡，帮我看下是不是 CPU 或内存太高"* → 会走 `host_resource_diagnosis` Skill，调用本机 psutil 采集真实数据
   - *"Redis 实例内存使用率 98%，客户端连接被强制断开"* → 走通用 OnCall，检索 Redis SOP + Prometheus 告警语料
3. 前端会 **实时 SSE 流式** 展示：Skill 选择 → 诊断计划生成 → 每一步工具调用 → Token 消耗 → 最终 Markdown 报告
4. `scripts/mock_alert.py --scenario redis` 模拟 Alertmanager Webhook 触发后台诊断

---

## 二、结构框架

```
multi_agent_github/
├── app/                          # 核心应用（FastAPI + Agent + RAG + Skill）
│   ├── agents/                   # LangGraph 智能体编排
│   │   ├── graph.py              # 图构建（核心编排逻辑）
│   │   ├── state.py              # 图状态定义（PlanExecuteState）
│   │   ├── planner.py            # 诊断计划生成器
│   │   ├── executor.py           # 执行器节点（调用工具）
│   │   ├── replanner.py          # 复盘/重规划节点
│   │   ├── fork_runner.py        # Fork 模式子图运行器
│   │   └── stream_sink.py        # SSE 流式输出桥接
│   ├── api/                      # API 路由层（v1）
│   ├── core/                     # 核心基础设施
│   │   ├── llm.py                # LLM 封装（DashScope / DeepSeek / Ollama 兜底）
│   │   ├── embedding.py          # Embedding 服务
│   │   ├── vector_store.py       # Milvus 向量存储
│   │   ├── hybrid_retriever.py   # BM25 + Vector Hybrid Search
│   │   ├── reranker.py           # 重排序（gte-rerank-v2）
│   │   ├── mcp_client.py         # MCP 客户端管理
│   │   └── web_search.py         # 联网搜索
│   ├── skills/                   # Skill 层（故障处理剧本）
│   │   ├── definitions/          # SKILL.md 定义文件
│   │   ├── router.py             # Skill 路由节点
│   │   ├── registry.py           # Skill 注册表
│   │   └── loader.py             # SKILL.md 解析器
│   ├── runtime/                  # 运行时控制
│   │   ├── tool_runner.py        # 并行工具执行器
│   │   ├── tool_filter.py        # Skill 工具白名单过滤
│   │   └── permissions.py        # 权限控制（read_only / ask / deny）
│   ├── services/                 # 业务服务层
│   │   ├── aiops_service.py      # AIOps 诊断服务（包装 SSE 事件流）
│   │   └── rag_service.py        # RAG 聊天服务
│   └── main.py                   # FastAPI 入口
├── mcp_servers/                  # 5 个 MCP 工具服务（独立进程）
├── frontend/                     # 前端（HTML + TailwindCSS + Vanilla JS）
├── docs/sop/                     # 内置 OnCall SOP（Redis/MySQL/通用）
├── data/kb_corpus/               # RAG 知识库（950+ Prometheus 告警语料）
└── scripts/                      # 知识库入库 / 告警模拟
```

---

## 三、核心底层机制 —— 多智能体协作

### 3.1 整体图结构（LangGraph 编排）

```text
[START]
   |
   v
+--------------+
| SkillRouter  |  --> 选 Skill（如 host_resource_diagnosis）
+------+-------+
       v
+----------+
| Planner  |  --> 基于 Skill Playbook 生成 4-6 步诊断计划
+-----+----+
      v
+----------+
| Executor |  --> 执行 plan[0]，调用 MCP 工具 / RAG 检索
+-----+----+
      v
+----------+
|Replanner |  --> 评估证据：继续 / 调整计划 / 生成报告 / 切换 Skill
+-----+----+
      |
 +----+----+
 | should  |
 |  end?   |   -- yes --> [END]
 +----+----+
     no
      v
  (loop back to Executor)
```

代码位置：`app/agents/graph.py`

### 3.2 四个核心节点的分工

| 节点 | 文件 | 职责 | 模型策略 |
|---|---|---|---|
| **Skill Router** | `app/skills/router.py` | 根据用户输入选择最匹配的 Skill | 轻量路由模型（flash），结构化输出 `SkillChoice` |
| **Planner** | `app/agents/planner.py` | 加载选中 Skill 的 Playbook，生成具体诊断步骤 | 轻量模型，结构化输出 `Plan` |
| **Executor** | `app/agents/executor.py` | 执行当前步骤，调用工具 | 执行模型，支持 **并行工具调用** |
| **Replanner** | `app/agents/replanner.py` | 复盘：继续 / 调整 / 终止出报告 / **Skill Reroute** | 轻量模型决策 + **重型模型润色报告** |

### 3.3 Skill-first 设计（关键创新）

**为什么先选 Skill？**
- 传统方式是把完整 SOP + 全部工具列表 + 用户问题一起塞给 Planner -> prompt 过长、工具噪声大、诊断步骤不可控
- **先收敛，再规划**：Router 先判断故障类型 -> 只加载该 Skill 的 Playbook + 工具白名单 -> Planner 在有限上下文中生成计划

**Skill 文件格式**（`app/skills/definitions/*/SKILL.md`）：
```yaml
---
name: host_resource_diagnosis
display_name: 主机资源诊断
description: CPU/内存/磁盘类故障
allowed_tools:
  - search_knowledge_base
  - get_local_system_overview
  - get_local_cpu_memory
  - list_top_processes
risk_level: low
---
# Playbook（Markdown 诊断剧本）
Phase 1: 先拍快照...
Phase 2-A: CPU 分支...
```

每个 Skill 只暴露相关工具，Executor 中通过 `filter_tools_for_skill()` 过滤，**LLM 看不到无关工具**，大幅降低误调用风险。

### 3.4 并行工具执行（Executor 核心）

代码位置：`app/runtime/tool_runner.py`

**传统 langchain.agents.create_agent 的问题是串行执行 tool_calls。**

本项目的 `run_parallel_agent` 实现了：
- **自动分批**：相邻的 `concurrency_safe` 工具合并成一批 `asyncio.gather` 并行执行
- **写工具切批**：遇到非 safe 工具（如 docker restart）自动切批，串行执行，保证顺序安全
- **异常隔离**：同批一个工具失败不影响兄弟工具
- **结果截断**：按 `ToolMeta.max_result_chars` 自动截断，防止 20KB 日志撑爆 context

**性能数据**：5 个只读工具并行，延迟从 **1.06s -> 0.22s**，加速 **4.88 倍**。

### 3.5 Replanner 的多层决策逻辑

代码位置：`app/agents/replanner.py`

Replanner 不是简单判断"够没够"，而是四层决策：

1. **快路径**：如果计划还剩很多步且上一步没失败 -> **跳过 LLM 调用**，直接继续，节省中间步骤的 1-3 秒延迟
2. **硬性兜底**：已达 `agent_max_steps` -> 强制生成报告
3. **Skill Reroute（Supervisor + Handoff）**：
   - 如果当前 Skill 的关键证据明确不成立（例：CPU Skill 但 CPU/内存/磁盘全部正常）
   - LLM 提议切换 Skill -> 代码层校验（次数上限 / 黑名单 / 证据门槛 / Skill 存在性）
   - 通过后清空旧计划，路由回 Planner 重新规划
   - 借鉴了 LangGraph Supervisor + Handoff + NeurIPS 2025 failure memory 思路，记录 `tried_skills` 防止回环
4. **报告生成**：`is_finished=True` 时，先用轻量模型出草稿，再用 **pro 模型重写** 高质量报告（5 段结构：问题概述 / 关键证据 / 根因分析 / 处置建议 / 结论）

### 3.6 状态流转与防死循环

状态定义：`app/agents/state.py` — `PlanExecuteState`

关键字段：
- `plan`: 待执行步骤列表
- `past_steps`: 已执行的 (步骤, 结果) 元组，用 `operator.add` 累加
- `response`: 最终报告（非空时触发 END）
- `iteration`: 当前步数（硬性上限）
- `reroute_count` / `tried_skills` / `pending_reroute`: Skill 切换控制
- `permission_mode`: 工具权限模式
- `transition_history`: 节点出口的结构化时间线（可观测）

### 3.7 SSE 流式输出（前端可观测）

`app/services/aiops_service.py` 把 `graph.astream()` 包装成 SSE 事件流：

| 事件类型 | 触发时机 |
|---|---|
| `start` | 诊断启动 |
| `skill_selected` | Router 选定 Skill |
| `plan` | Planner 生成计划 |
| `step_start` / `step_complete` | Executor 执行步骤 |
| `tool_call` | 工具调用完成（含耗时、结果长度） |
| `usage` | Token 消耗（input/output/total，含 DeepSeek cache hit） |
| `replan` | Replanner 调整计划 |
| `report` | 最终报告 |
| `transition` | 节点状态转换记录 |
| `complete` / `error` | 结束或异常 |

前端通过这些事件实时渲染诊断时间线、Token 监控卡片、工具调用详情。

---

## 四、其他补充亮点

### 4.1 RAG 知识库（不是简单向量检索）

- **规模**：954 个文档 / 4080 个 chunks，覆盖 Prometheus 告警语料（MySQL、Redis、K8s、Docker 等）+ 内置 SOP
- **Hybrid Search**：BM25（稀疏）+ Vector（稠密）+ RRF 融合，解决向量检索在精确错误码（如 `ERR_CONN_REFUSED`）和罕见长尾词上的失手
- **Reranker**：`gte-rerank-v2` 对召回结果二次排序
- **评估数据**：R@1 从 83.33% -> 91.67%，MRR 从 0.882 -> 0.938

### 4.2 Token 与性能优化（有量化数据）

| 指标 | 优化前 | 优化后 | 降幅 |
|---|---|---|---|
| Planner prompt tokens | 9098 | 575 | **-93.5%** |
| 全链路 prompt tokens | 10526 | 2450 | **-76.7%** |
| 全链路 total tokens | 11889 | 3988 | **-66.5%** |

核心手段：**Skill-first 减少无关 prompt**，只加载选中 Skill 的 Playbook 和工具描述。

### 4.3 权限与风险控制

- `PermissionMode` 四层：`read_only` / `normal` / `ask_destructive` / `bypass`
- 每个 Skill 有 `risk_level`（low/medium/high）
- 每个工具有 `read_only` 标记，写操作（如 docker restart）默认不自动执行
- Executor 中 `decisions` 控制每个工具的 allow/ask/deny 行为

### 4.4 模型分层策略（成本控制）

不同节点用不同模型，不是一刀切：
- **Router / Planner / Replanner（决策）**：轻量模型（flash），只求结构化输出
- **Executor（工具调用）**：中等模型，需要理解工具返回
- **Report（报告润色）**：pro 模型，只求质量不求速度

### 4.5 真实系统数据 vs 编造数据

这是项目反复强调的工程纪律：
- 本机系统工具（psutil）返回的是 **真实实时数据**
- Skill Playbook 中明确要求：**禁止编造工具未返回的内容**
- 知识库 SOP 仅作排查思路参考，**不能作为本机现状的证据**

### 4.6 可扩展性

- **新增 Skill**：在 `app/skills/definitions/` 下新建目录 + `SKILL.md`，重启自动注册
- **新增 MCP 工具**：在 `mcp_servers/` 下新建服务，`.env` 加端点，代码自动加载
- **知识库更新**：`scripts/ingest_kb_corpus.py` 一键重新入库

---

## 五、关闭服务

1. 在运行 run.ps1 的终端里按 Ctrl+C

2. 在同一终端执行：powershell -NoProfile -ExecutionPolicy Bypass -File .\run.ps1 -Stop

   这会杀掉所有 MCP 服务和残留进程。

3. docker compose down/stop

   
